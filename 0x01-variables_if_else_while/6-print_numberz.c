#include <stdio.h>
/*
 * main function starts entry of code
 * return 0 if successful
 * Displays number without using printf
 */
int main(void)
{
	int n=48;

	while(n <= 57)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
