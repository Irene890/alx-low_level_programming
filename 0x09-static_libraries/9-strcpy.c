#include "main.h"
#include <string.h>

/**
 * strcpy - Function
 * @dest: character
 * @src: source character
 * Return: Returns strcpy
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
