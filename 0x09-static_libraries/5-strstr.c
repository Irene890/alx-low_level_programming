#include "main.h"
#include <string.h>

/**
 * _strstr - Function
 * @haystack: character
 * @needle: character
 * Return: Returns strstr
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
