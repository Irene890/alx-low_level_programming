#include "main.h"

/**
 * _abs - calculates the absolute value
 * @m: is an integer
 * Return: returns the value
 */
int _abs(int m)
{
	if (m >= 0)
	{
		return (m);
	}
	else
	{
		return (m * -1);
	}
}
